<div class="fos_user_user_show">
    <div><b>{{ 'profile.show.username'|trans({}, 'FOSUserBundle') }}</b> <span>{{ user.username }}</span></div>
    <div><b>{{ 'profile.show.email'|trans({}, 'FOSUserBundle') }}</b> <span>{{ user.email }}</span></div>
</div>
